import { createContext } from "react";

//This will be used in the future for passing data between pages. May not be necessary to keep.

const JavaEduDataContext = createContext([]);

export default JavaEduDataContext;